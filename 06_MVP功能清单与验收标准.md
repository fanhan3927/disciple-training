# MVP 功能清单与验收标准

## 1. MVP 定义

四周内让 3–5 个熟人小组在海外 Web/PWA 上完整跑通：

邀请加入 → 多小组切换 → 按周课程 → 每日任务 → 私人/组内分享 → Leader 跟进 → 受控 AI 问答 → 四周复盘。

## 2. P0 必须上线

### P0-1 账号、同意与个人设置

功能：

- 邮箱注册/登录/退出；
- 18 岁以上确认；
- 服务条款、隐私和敏感数据明确同意；
- 显示名、头像、时区；
- 修改密码/邮件恢复；
- 数据导出与账号删除申请入口。

验收：

- 未同意敏感数据处理不能加入小组；
- 同意记录保存版本、时间和来源；
- 通知不泄露课程或祷告正文；
- 账号删除入口不隐藏在客服流程后。

### P0-2 邀请制小组

功能：

- Leader 创建、编辑、暂停、归档小组；
- 小组人数上限；
- 限时/限次邀请；
- 用户接受邀请；
- Leader 查看、移除成员；
- 成员退出；
- 同一用户多小组、按组角色。

验收：

- 不存在公开小组目录；
- 无邀请不可加入；
- A 组成员无法读取 B 组；
- Leader 权限不跨组；
- 邀请撤销后立即失效。

### P0-3 课程与四周排期

功能：

- 一套已审核“新生活重启营”；
- 4 周、每周 6–7 个微任务；
- 任务类型：阅读、反思、操练、经文记忆、自评；
- Leader 选择开始日期；
- 按时区解锁；
- 个人完成状态；
- 周进度和总进度。

验收：

- 课程内容带来源和版本；
- 修改发布版本不破坏既有学习记录；
- 用户可跳过任务；
- 不显示组员排名；
- 涉及本地教会的提醒按课出现。

### P0-4 私人笔记与可见范围

功能：

- 每个任务可保存私人笔记；
- 用户可选择分享给当前组或仅 Leader；
- 发布前显示可见者；
- 编辑、删除、撤回；
- 30 秒误发撤回。

验收：

- 默认 `private`；
- Leader 无法读取私人正文；
- 切组后目标组清晰可见；
- 跨组分享需逐组确认；
- 删除后普通接口无法读取。

### P0-5 组内讨论

功能：

- 按周讨论主题；
- 文本帖子和评论；
- 简单回应：鼓励、愿意代祷；
- 举报；
- Leader 隐藏不当内容；
- 禁止站外公开分享按钮。

验收：

- 仅当前组成员可读写；
- 被暂停成员不可发帖；
- 删除/隐藏动作有审计；
- 评论通知不包含敏感正文；
- 富文本经过 XSS 清理。

### P0-6 代祷

功能：

- 创建代祷事项；
- `private`、`leader_only`、`group` 三种范围；
- 过期时间；
- 标记继续/结束/已回应；
- 删除和修改范围。

验收：

- 默认仅自己；
- 明示避免披露第三人隐私；
- AI 默认不读取；
- 不提供公开祷告墙；
- 组外用户绝对不可读。

### P0-7 Leader 面板

功能：

- 本组成员和角色；
- 本周开始/完成状态；
- 成员主动共享内容；
- `leader_only` 请求；
- 组级活跃与完成率；
- 发布真人周总结；
- 外部会议链接。

验收：

- 无私人笔记或 AI 对话入口；
- 组级摘要不显示敏感正文；
- 会议不自动录制；
- Leader 无法导出其他小组数据；
- 周报 AI 草稿若启用，只用聚合数据。

### P0-8 受控 AI 课程问答

功能：

- 仅课程内入口；
- 检索已发布且 `ai_approved` 的内容；
- 回答带课程出处；
- 来源不足拒答；
- 风险分类；
- 高风险边界提示；
- 用户选择是否保存为私人笔记。

验收：

- 不把私人笔记自动加入上下文；
- 引用可点击回到课程块；
- 无来源不生成确定性答案；
- “神要我……”测试集不产生神谕；
- 圣礼问题固定转向本地教会；
- AI 无修改权限、无发帖权限。

### P0-9 举报、安全与审计

功能：

- 举报帖子、成员、小组；
- 安全案件状态；
- 有限审核访问；
- 隐藏、暂停、冻结邀请；
- 管理操作审计。

验收：

- 安全人员读取关联案件并留痕；
- 普通管理员不能随意浏览 D3/D4 正文；
- 即时危险提示不承诺平台实时响应；
- 举报者能看到基本状态；
- 审计记录不能由普通管理员修改。

### P0-10 法律与边界页面

功能：

- 产品使命；
- 信仰宣言；
- 平台非教会声明；
- 圣礼边界；
- AI 说明；
- 隐私政策；
- 服务条款；
- 社区守则；
- Leader 盟约。

验收：

- 版本化；
- 用户同意关联具体版本；
- 圣礼相关课程页显示强提醒；
- 页面从页脚和设置可访问。

## 3. P1 内测后优先

| 功能 | 进入条件 |
|---|---|
| PWA 安装与离线缓存课程壳 | 用户明确需要，且不缓存敏感正文 |
| 邮件/站内温和提醒 | 用户主动开启 |
| 经文记忆卡与间隔复习 | 首轮完成率证明有价值 |
| Leader 助手生成讨论提纲 | 仅用课程和组级聚合数据 |
| 多语言界面 | 海外华人以外出现真实需求 |
| YouVersion Bible Reader/SDK | 完成接入申请与许可确认 |
| 课程后台编辑器 | 不再由开发者手工导入时 |
| 数据导出自助化 | 首轮人工流程验证完成 |
| 小组接力与下一课程 | 四周完成率达到门槛 |

## 4. P2 暂缓

- 原生 iOS/Android；
- 自建音视频、转录和会议归档；
- 公开社区和发现；
- 陌生人组队；
- AI 语音陪伴；
- Leader Marketplace；
- 多机构白标；
- 复杂积分、排行榜、徽章商城；
- 支付订阅；
- 自动翻译全部课程；
- AI 对成员的成长评分。

## 5. 页面清单

### 公共页面

- `/`
- `/mission`
- `/beliefs`
- `/boundaries`
- `/privacy`
- `/terms`
- `/community-guidelines`
- `/invite/[token]`
- `/login`
- `/signup`

### 登录后

- `/home`
- `/groups`
- `/groups/[groupId]`
- `/groups/[groupId]/week/[weekId]`
- `/groups/[groupId]/lesson/[lessonId]`
- `/groups/[groupId]/discussion`
- `/groups/[groupId]/prayers`
- `/groups/[groupId]/members`
- `/groups/[groupId]/leader`
- `/groups/[groupId]/settings`
- `/ai/threads/[threadId]`
- `/notifications`
- `/settings/profile`
- `/settings/privacy`
- `/settings/data`
- `/help/report`

### 平台后台

- `/admin/content`
- `/admin/content/[versionId]`
- `/admin/safety`
- `/admin/safety/[caseId]`
- `/admin/audit`

## 6. 非功能要求

| 类别 | MVP 要求 |
|---|---|
| 性能 | 主要页面移动网络下可用；P75 LCP 目标 < 2.5s |
| 可用性 | 移动端优先；主要任务单手可完成 |
| 无障碍 | 语义 HTML、键盘操作、清晰焦点、颜色不作唯一信息载体 |
| 国际化 | UI 文案使用 i18n key；时间按用户时区 |
| 安全 | RLS、MFA 管理员、速率限制、CSRF/XSS/输入验证 |
| 隐私 | 默认私密、无正文分析日志、AI 情境同意 |
| 可观测性 | 只记录技术与枚举事件，不记录敏感正文 |
| 可靠性 | 数据库每日托管备份；上线前恢复演练 |
| 内容 | 课程版本化；发布需要审核状态 |
| 成本 | 首轮尽量使用免费额度；AI 设置用户/日配额 |

## 7. 指标定义

| 指标 | 定义 | 目标 |
|---|---|---|
| 邀请转化 | 接受邀请并注册 / 有效邀请打开 | ≥ 60% |
| 激活 | 注册后 72h 内加入组并完成首个任务 | ≥ 70% |
| W1 参与 | 第一周完成 ≥4 微任务 / 激活用户 | ≥ 50% |
| 课程完成 | 完成四周核心任务 / 激活用户 | ≥ 40% |
| 小组互动 | 每活跃组每周有意义回应数 | ≥ 3 |
| AI 有据率 | 含有效课程引用的正常回答 | ≥ 80% |
| AI 拒答准确 | 高风险测试集中正确边界处理 | 100% 阻断关键红线 |
| 本地教会增强 | 回访中认为更愿意参与本地教会 | ≥ 60% |
| 跨组泄露 | 经测试或真实发现 | 0 |
| 私密内容误送 AI | 未经当次确认的事件 | 0 |

## 8. 功能完成定义

一个 P0 功能只有同时满足以下条件才算完成：

- 正常路径可用；
- 空状态、错误状态、权限拒绝状态已设计；
- 移动端测试通过；
- 服务端授权和 RLS 测试通过；
- 无障碍基本检查通过；
- 不记录敏感正文；
- 文案包含必要信仰/隐私边界；
- 单元、集成和端到端关键测试通过；
- 有回滚或关闭开关；
- README 和迁移说明已更新。

