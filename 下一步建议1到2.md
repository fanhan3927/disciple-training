# 下一步建议：从 M1 到 M2

## 1. 当前状态

项目已经完成 **M1｜认证、同意与边界**：

- Supabase helper 已建立；
- `profiles`、`legal_documents`、`user_consents` migration 已建立；
- 基础 RLS 策略已建立；
- 注册、登录、退出、密码恢复入口已实现；
- 成人确认和必需同意流程已实现；
- 版本化使命、信仰、边界、隐私、条款、社区守则和 AI 说明页面已实现；
- `/home` 会阻断未完成成人确认或必需同意的用户；
- 数据权利入口已预留；
- 小组、邀请、课程、讨论、代祷和 AI 尚未实现。

下一步建议进入 **M2｜邀请制多小组**。

## 2. M2 总目标

建立邀请制熟人小组的最小闭环，并确保多小组权限隔离由数据库和服务端共同强制。

M2 完成后，用户应能够：

- Leader 创建小组；
- Leader 编辑小组基础信息；
- Leader 接受 Leader 盟约；
- Leader 生成限时、限次邀请；
- 受邀用户打开邀请预览；
- 受邀用户接受邀请并加入小组；
- 用户在多个小组之间切换；
- Leader 查看本组成员；
- Leader 移除本组成员；
- 成员主动退出小组；
- Leader 归档小组；
- 邀请被撤销、过期或达到使用上限后立即失效；
- A 组成员无法读取 B 组数据；
- A 组 Leader 不能管理 B 组。

## 3. 建议优先级

### 3.1 先确认 Supabase 环境

M2 依赖真实数据库权限验证。进入 M2 前建议先确认：

- 是否已有 Supabase 项目；
- 是否使用本地 Supabase 进行开发；
- M1 migration 是否已应用；
- `.env.local` 是否包含 Supabase URL 和 anon key；
- `SUPABASE_SERVICE_ROLE_KEY` 是否只存在于服务端；
- 是否能执行数据库测试或至少执行 migration 静态测试。

建议先运行：

```bash
npm run lint
npm run typecheck
npm run test
npm run build
```

### 3.2 创建小组数据模型

建议新增 migration：

```text
supabase/migrations/202607300002_m2_groups_invitations.sql
```

建议创建枚举：

- `group_role`
- `membership_status`
- `group_lifecycle_status`
- `group_safety_status`

建议创建表：

- `groups`
- `group_memberships`
- `group_invitations`

`groups` 最小字段：

- `id`
- `name`
- `description`
- `timezone`
- `member_limit`
- `lifecycle_status`
- `safety_status`
- `created_by`
- `created_at`
- `updated_at`
- `archived_at`

`group_memberships` 最小字段：

- `group_id`
- `user_id`
- `role`
- `status`
- `invited_by`
- `joined_at`
- `left_at`
- `leader_covenant_version`
- `accepted_covenant_at`
- `created_at`
- `updated_at`

主键建议：

```sql
primary key (group_id, user_id)
```

`group_invitations` 最小字段：

- `id`
- `group_id`
- `token_hash`
- `role_to_grant`
- `expires_at`
- `max_uses`
- `use_count`
- `created_by`
- `revoked_at`
- `created_at`

关键要求：

- 只保存邀请 token hash；
- 原始 token 只在创建时展示；
- 邀请不能泄露成员列表；
- 小组没有公开目录；
- 每个活跃小组必须至少有一个 Leader；
- 删除或移除唯一 Leader 前必须先移交或归档。

### 3.3 再实现 RLS

M2 的核心是 RLS，不建议先做 UI。

建议实现策略：

#### `groups`

- 活跃成员可以读取自己所在小组；
- Leader 可以更新自己小组基础信息；
- 归档小组后普通成员仍只能看到必要历史状态；
- 非成员不能读取；
- 不提供公开小组目录。

#### `group_memberships`

- 活跃成员可以读取本组成员列表；
- Leader 可以管理本组成员状态；
- 普通成员不能修改其他成员；
- 成员可以将自己的状态改为 `left`；
- 被移除或退出后立即失去本组读取权限；
- 其他组 Leader 不能读取或管理本组成员。

#### `group_invitations`

- Leader 可以创建、查看、撤销本组邀请；
- 普通成员不能创建邀请；
- 邀请预览必须通过受控服务端函数读取；
- 接受邀请必须通过受控函数；
- 过期、撤销、达到上限的邀请不可接受；
- token hash 不对普通客户端暴露。

### 3.4 实现服务端领域函数

建议先实现服务端函数，不要把业务规则散落在页面组件里。

建议函数：

- `createGroup`
- `updateGroup`
- `archiveGroup`
- `acceptLeaderCovenant`
- `createInvitation`
- `previewInvitation`
- `acceptInvitation`
- `revokeInvitation`
- `listUserGroups`
- `listGroupMembers`
- `removeMember`
- `leaveGroup`
- `switchCurrentGroup`

建议封装权限 helper：

- `assertGroupMember`
- `assertActiveGroupMember`
- `assertGroupLeader`
- `assertCanManageMembership`
- `assertInviteCanBeUsed`

### 3.5 再实现页面

建议页面：

- `/groups`
- `/groups/new`
- `/groups/[groupId]`
- `/groups/[groupId]/members`
- `/groups/[groupId]/settings`
- `/invite/[token]`

建议最小流程：

1. 已完成 M1 onboarding 的用户进入 `/groups`；
2. 若无小组，显示空状态；
3. 用户可创建第一个小组并成为 Leader；
4. Leader 必须接受 Leader 盟约；
5. Leader 创建邀请；
6. 受邀用户打开 `/invite/[token]`；
7. 邀请预览只显示小组名称、Leader 显示名、过期时间，不显示成员列表；
8. 用户接受邀请后进入小组；
9. 顶部显示当前小组；
10. 用户可以切换小组。

### 3.6 最后补测试

M2 必须建立固定权限矩阵：

- Alice：A 组 Leader、B 组 Member；
- Bob：A 组 Member；
- Carol：B 组 Leader；
- Dave：未加入任何组。

建议测试覆盖：

- Alice 能管理 A 组；
- Alice 不能管理 B 组；
- Carol 不能读取 A 组；
- Bob 能读取 A 组但不能管理成员；
- Dave 无邀请不能加入任何组；
- Bob 退出 A 组后立即无法读取 A 组；
- 撤销邀请后不能接受；
- 过期邀请不能接受；
- 达到使用上限后不能接受；
- token hash 不通过普通接口返回；
- 被暂停成员不能发起后续写操作。

## 4. M2 不建议提前做的内容

M2 阶段仍然不建议做：

- 课程内容导入；
- 每日任务；
- 私人灵修记录；
- 组内讨论；
- 代祷；
- Leader 进度面板；
- AI 问答；
- 数据导出 ZIP；
- 正式付费；
- 公开社区或公开小组目录。

原因：

- 课程和任务依赖小组上下文；
- 讨论和代祷依赖组内权限；
- AI 依赖课程审核模型；
- 公开社区违反 MVP 产品边界。

## 5. 验收标准

M2 完成建议至少满足：

- `npm run lint` 通过；
- `npm run typecheck` 通过；
- `npm run test` 通过；
- `npm run build` 通过；
- 数据库 migration 可应用；
- `groups`、`group_memberships`、`group_invitations` 均开启 RLS；
- 无公开小组目录；
- 无邀请不能加入小组；
- 邀请只保存 token hash；
- 邀请撤销后立即失效；
- 邀请过期后不可接受；
- 邀请达到最大使用次数后不可接受；
- 同一用户可加入多个小组；
- 角色按小组隔离；
- A 组成员无法读取 B 组；
- Leader 权限不跨组；
- 成员退出后立即失去本组读取权限；
- Leader 不能移除本组唯一 Leader，除非归档或先移交；
- 页面有正常、空、错误和权限拒绝状态；
- 移动端 360px 无横向溢出；
- 日志不记录邀请原始 token、邮箱或敏感正文。

## 6. 安全与隐私重点

M2 是 MVP 中权限风险最高的阶段之一。

必须重点关注：

- IDOR：不能通过猜测 `groupId` 读取其他小组；
- 邀请 token：数据库只存 hash；
- 邀请预览：不泄露成员列表；
- Leader 权限：只能在本组生效；
- 多小组身份：同一用户在不同组可以有不同角色；
- 退出撤权：成员退出或被移除后权限立即失效；
- 暂停状态：暂停成员不能继续写入；
- 审计预留：Leader 移除成员、归档小组、撤销邀请应后续进入 audit trail；
- 前端隐藏按钮不能代替服务端和 RLS 权限。

## 7. 人工确认项

进入 M2 前建议确认：

- 是否已有真实 Supabase 项目；
- 是否使用本地 Supabase CLI；
- Leader 盟约正式文本；
- 小组默认人数上限；
- 小组最大人数上限；
- 邀请链接默认有效期；
- 邀请链接最大有效期；
- 邀请默认最大使用次数；
- 是否允许 co-leader；
- observer 角色是否在 MVP 启用；
- 小组归档后成员可见范围；
- 成员退出后是否保留其历史归属记录；
- 首批内测是否使用真实邮箱，还是继续使用合成测试账号。

## 8. 推荐执行提示词

后续可以直接使用以下任务说明开始 M2：

```text
请执行 M2｜邀请制多小组。

开始前：
1. 读取 AGENTS.md、README、docs/M1_AUTH_CONSENT_BOUNDARIES_REPORT.md、docs/product、docs/adr 和 下一步建议1到2.md。
2. 检查当前仓库状态，保留现有修改。
3. 先只读检查当前 Supabase helper、M1 migration、认证页面和测试配置。
4. 给出不超过 8 项的实施计划。

实施范围：
- 新增 groups、group_memberships、group_invitations migration。
- 为所有小组相关表开启 RLS。
- 实现 Leader 创建小组、编辑小组、归档小组。
- 实现 Leader 盟约确认。
- 实现限时、限次邀请创建、预览、接受、撤销。
- 邀请 token 只保存 hash，原始 token 只在创建时显示。
- 实现用户多小组列表和当前小组切换。
- 实现成员列表、成员退出、Leader 移除成员。
- 实现服务端权限 helper，不把业务权限只放在前端。
- 补齐 Alice/Bob/Carol/Dave 权限矩阵测试。
- 补齐关键 E2E：Leader 建组、邀请 Bob、Bob 加入、Dave 使用过期链接失败。

不要：
- 不实现课程、任务、讨论、代祷、Leader 进度面板或 AI。
- 不创建公开小组目录。
- 不暴露邀请 token hash。
- 不把 service_role 暴露到客户端。
- 不记录邀请原始 token、邮箱或敏感正文到日志。

验证：
- npm run lint
- npm run typecheck
- npm run test
- npm run build
- 运行 Alice/Bob/Carol/Dave 越权测试
- 运行邀请流程 E2E

完成后输出：
- 实现结果
- 变更文件
- 数据库迁移
- 测试结果
- 安全/隐私影响
- 已知限制
- 下一步建议
```

