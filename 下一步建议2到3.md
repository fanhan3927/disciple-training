# 下一步建议：从 M2 到 M3

## 1. 当前状态

项目已经完成 **M2｜邀请制多小组**：

- `groups`、`group_memberships`、`group_invitations` migration 已建立；
- 小组、成员、邀请相关 RLS 已建立；
- Leader 创建、编辑、归档小组流程已实现；
- Leader 盟约确认已实现；
- 邀请创建、预览、接受、撤销已实现；
- 邀请 token 只保存 `sha256` hash；
- 用户可以查看自己的多个小组；
- 用户可以进入当前小组详情；
- Leader 可以查看和移除本组成员；
- 成员可以主动退出小组；
- 无公开小组目录；
- 小组角色按 `group_id` 隔离；
- 课程、任务、进度、私人记录、讨论、代祷和 AI 尚未实现。

下一步建议进入 **M3｜课程、任务与私人记录**。

## 2. M3 总目标

建立四周课程学习闭环的最小版本，并确保私人记录默认私密、课程内容可版本化、学习进度可追踪。

M3 完成后，用户应能够：

- 在已加入的小组中看到当前课程；
- Leader 为小组创建一个课程班次；
- Leader 设置课程开始日期；
- 系统按四周排期解锁课程；
- 用户查看每周课程和每日微任务；
- 用户完成或跳过任务；
- 用户查看个人周进度和总进度；
- 用户为任务保存私人记录；
- 用户可将记录分享给当前组或仅 Leader；
- 用户可撤回分享；
- 用户可删除记录；
- Leader 只能看到用户主动分享的内容；
- Leader 不能读取私人记录正文；
- 课程内容具备来源、版本和审核状态；
- 不导入未经确认授权的经文正文、图片、诗歌或外链。

## 3. 建议优先级

### 3.1 先确认课程授权与内容边界

M3 开始前必须确认：

- 《新生活》前四课是否已取得产品化使用授权；
- 是否允许改编为每日微任务；
- 是否允许用于 AI/RAG；
- 是否允许商业化；
- 是否允许再分发；
- 是否允许在 Web/PWA 中展示；
- 是否已有神学审核负责人；
- 是否已有版权审核负责人；
- 首发圣经译本是否已确认授权；
- 未确认圣经译本前是否只显示经文引用，不显示译文正文。

若授权未确认，建议：

- 只创建课程数据结构；
- 只导入产品化标题、任务提示和来源定位；
- 不导入教材全文；
- 不导入经文译文正文；
- 不导入诗歌、图片、音视频或第三方文章正文。

### 3.2 创建课程内容模型

建议新增 migration：

```text
supabase/migrations/202607300003_m3_courses_tasks_entries.sql
```

建议创建枚举：

- `course_status`
- `course_version_status`
- `review_type`
- `review_decision`
- `lesson_risk_level`
- `content_block_type`
- `content_review_status`
- `task_type`
- `task_completion_status`
- `cohort_status`
- `entry_type`
- `entry_visibility`
- `entry_status`
- `share_scope`

建议创建课程表：

- `content_sources`
- `courses`
- `course_versions`
- `modules`
- `lessons`
- `content_blocks`
- `scripture_refs`
- `content_reviews`
- `ai_policies`

关键要求：

- 课程版本发布后不可直接修改；
- 内容块必须保留 `source_locator`；
- 内容块必须标记版权、神学和安全审核状态；
- `ai_approved` 默认 false；
- 高风险内容默认不允许 AI；
- 未确认授权的圣经译文正文不进入 `licensed_text`；
- 课程发布必须依赖审核状态。

### 3.3 创建班次、任务与进度模型

建议创建：

- `cohorts`
- `cohort_lessons`
- `tasks`
- `task_completions`

`cohorts` 最小字段：

- `id`
- `group_id`
- `course_version_id`
- `starts_on`
- `status`
- `meeting_url`
- `meeting_schedule`
- `created_by`
- `created_at`
- `updated_at`

`cohort_lessons` 最小字段：

- `cohort_id`
- `lesson_id`
- `unlock_at`
- `due_at`

`tasks` 最小字段：

- `id`
- `lesson_id`
- `task_type`
- `title`
- `prompt_json`
- `is_core`
- `sequence`
- `estimated_minutes`

`task_completions` 最小字段：

- `task_id`
- `user_id`
- `cohort_id`
- `status`
- `completed_at`
- `reflection_entry_id`
- `created_at`
- `updated_at`

关键要求：

- 完成记录不保存反思正文；
- 用户可以完成或跳过任务；
- 不显示组员排名；
- 进度仅本人和本组 Leader 的有限摘要可见；
- 课程解锁按用户时区显示；
- Leader 设置开始日期后生成四周解锁计划。

### 3.4 创建私人记录与分享模型

建议创建：

- `entries`
- `content_shares`

`entries` 最小字段：

- `id`
- `author_id`
- `group_id`
- `cohort_id`
- `lesson_id`
- `task_id`
- `entry_type`
- `visibility`
- `body`
- `status`
- `expires_at`
- `created_at`
- `updated_at`
- `deleted_at`

`content_shares` 最小字段：

- `entry_id`
- `target_group_id`
- `share_scope`
- `shared_at`
- `revoked_at`

关键要求：

- 记录默认 `private`；
- Leader 不能读取私人正文；
- 用户主动分享后，目标范围才可见；
- 分享给组或 Leader 前必须显示可见者；
- 切组后发布必须明确目标组；
- 撤回分享后立即失效；
- 删除后普通接口不可读取；
- AI 默认不能读取私人记录；
- 日志和测试快照不能写入私人正文。

### 3.5 再实现服务端领域函数

建议按领域拆分：

```text
src/features/courses/
src/features/tasks/
src/features/entries/
```

建议课程函数：

- `listPublishedCourses`
- `getCourseVersion`
- `getLesson`
- `getLessonTasks`
- `assertCoursePublished`
- `assertContentReviewed`

建议班次函数：

- `createCohort`
- `updateCohortStartDate`
- `generateCohortLessons`
- `getGroupCohort`
- `assertGroupLeaderCanManageCohort`

建议任务函数：

- `listWeekLessons`
- `getTask`
- `completeTask`
- `skipTask`
- `resetTaskCompletion`
- `getUserProgress`
- `getLeaderProgressSummary`

建议私人记录函数：

- `createEntry`
- `updateEntry`
- `deleteEntry`
- `shareEntry`
- `revokeEntryShare`
- `listMyEntries`
- `listSharedEntriesForGroup`
- `listLeaderOnlyRequests`

服务端必须校验：

- 当前用户是目标小组活跃成员；
- 当前课程班次属于目标小组；
- 当前任务属于目标课程版本；
- 当前记录作者是当前用户；
- 当前分享目标是用户所在小组；
- Leader 只能读主动分享内容。

### 3.6 再实现页面

建议页面：

- `/groups/[groupId]/week/[weekId]`
- `/groups/[groupId]/lesson/[lessonId]`
- `/groups/[groupId]/settings` 中增加课程班次设置；
- `/groups/[groupId]` 中增加当前课程进度入口；

可暂缓页面：

- 课程后台编辑器；
- 批量内容管理；
- Leader 完整进度面板；
- AI 课程问答；
- 讨论和代祷。

建议最小用户流程：

1. Leader 进入小组设置；
2. Leader 选择已发布课程版本；
3. Leader 设置课程开始日期；
4. 系统生成四周课程排期；
5. 成员进入小组首页；
6. 成员看到本周课程；
7. 成员进入课次页；
8. 成员完成或跳过任务；
9. 成员保存私人记录；
10. 成员选择是否分享给当前小组或仅 Leader；
11. 成员撤回分享；
12. Leader 只能看到主动分享内容和有限进度摘要。

## 4. M3 不建议提前做的内容

M3 阶段仍然不建议做：

- 组内讨论；
- 代祷完整流程；
- Leader 完整面板；
- AI 问答；
- RAG 向量库；
- 内容后台编辑器；
- 邮件提醒；
- 数据导出 ZIP；
- 公开社区；
- 公开课程市场；
- 支付订阅。

原因：

- 讨论和代祷依赖私人内容可见范围模型；
- AI 依赖课程审核状态和高风险分类；
- Leader 面板依赖任务和分享数据稳定；
- 内容后台编辑器可在手工 seed 验证后再做。

## 5. RLS 建议

### 5.1 课程读取

普通用户只可读取：

- 已发布课程；
- 已发布课程版本；
- 已发布版本下的模块、课次、任务和内容块。

建议规则：

```sql
course_versions.status = 'published'
```

内容块还需满足：

```sql
copyright_status = 'approved'
and theology_status = 'approved'
```

AI 相关读取还必须额外满足：

```sql
ai_approved = true
```

### 5.2 班次读取

用户可读取班次的条件：

- 用户是该班次所属小组的 active member；
- 小组未归档；
- 班次状态不是 deleted。

Leader 可管理班次的条件：

- 用户是该小组 active leader 或 co_leader。

### 5.3 任务完成记录

用户可读取：

- 自己的完成记录；
- 本组 Leader 可读取有限摘要。

建议避免 Leader 直接读取所有用户任务详情正文，因为完成记录不应包含私人反思正文。

### 5.4 私人记录

`entries` 默认只允许：

- 作者读取；
- 作者更新；
- 作者删除或软删除。

主动分享通过 `content_shares` 控制：

- `group`：当前目标小组 active members 可读；
- `leader_only`：当前目标小组 active leader/co_leader 可读；
- `private`：仅作者可读。

重要：

- 不要用一个宽松的 `entries.visibility` 策略让 Leader 自动看到私人正文；
- 分享撤回后必须立即失效；
- 删除后普通查询不可见；
- 其他组 Leader 任何时候不可读。

## 6. 测试建议

### 6.1 固定身份矩阵

继续沿用 M2 的四身份：

- Alice：A 组 Leader、B 组 Member；
- Bob：A 组 Member；
- Carol：B 组 Leader；
- Dave：未加入任何组。

### 6.2 课程与任务测试

建议覆盖：

- A 组成员能读取 A 组班次；
- Carol 不能读取 A 组班次；
- Dave 不能读取任何组班次；
- Bob 能完成自己的任务；
- Bob 不能替 Alice 完成任务；
- Alice 作为 A 组 Leader 能读取 A 组有限进度摘要；
- Alice 作为 B 组 Member 不能读取 B 组 Leader 摘要；
- 已发布课程版本可读；
- draft/review 课程版本普通用户不可读；
- 课程版本发布后不可变更；
- 任务完成记录不保存反思正文。

### 6.3 私人记录测试

建议覆盖：

- Bob 创建私人记录后 Alice 不能读取正文；
- Bob 主动分享给 A 组后 Alice 能读取；
- Bob 分享给 `leader_only` 后 Alice 能读取，普通 A 组成员不能读取；
- Bob 撤回分享后 Alice 立即不能读取；
- Carol 任何时候不能读取 Bob 在 A 组的记录；
- Dave 不能通过猜 ID 读取记录；
- 删除记录后普通接口不可读；
- 切组后分享必须重新确认目标组。

### 6.4 安全测试

建议覆盖：

- IDOR：猜测 `lessonId`、`cohortId`、`entryId` 不得越权；
- XSS：记录正文和任务回应必须清理或安全渲染；
- 日志扫描：不得出现灵修、祷告、AI 问答正文；
- 课程授权：未审核内容不能发布；
- AI 准入：`ai_approved=false` 的内容不能进入 AI 检索。

## 7. 验收标准

M3 完成建议至少满足：

- `npm run lint` 通过；
- `npm run typecheck` 通过；
- `npm run test` 通过；
- `npm run build` 通过；
- M3 migration 可应用；
- 课程内容表开启 RLS；
- 班次、任务、完成记录表开启 RLS；
- 私人记录和分享表开启 RLS；
- 已发布课程版本可被小组成员读取；
- draft/review 课程版本普通用户不可读；
- Leader 可以为本组创建课程班次；
- Leader 不能为其他组创建班次；
- 用户可以完成或跳过自己的任务；
- 用户不能替他人完成任务；
- 用户可以保存私人记录；
- 私人记录默认 private；
- Leader 无法读取私人正文；
- 用户主动分享后目标范围可见；
- 撤回分享后立即失效；
- 删除后普通接口不可读；
- 不显示组员排行榜；
- 进度摘要不包含敏感正文；
- 不导入未授权经文译文正文；
- 页面有正常、空、错误和权限拒绝状态；
- 移动端 360px 无横向溢出；
- README 或阶段报告更新。

## 8. 安全与隐私重点

M3 会首次引入用户敏感正文，因此安全和隐私优先级高于页面完整度。

必须重点关注：

- 私人记录默认私密；
- Leader 不得自动读取私人正文；
- AI 默认不得读取私人记录；
- 私人记录不得进入课程 RAG；
- 日志不得包含灵修、反思、祷告或 AI 问答正文；
- 分享前必须清晰显示可见者；
- 跨组分享必须逐组确认；
- 删除和撤回必须立即生效；
- 课程发布必须依赖审核状态；
- 未授权内容不能进入产品或 AI 检索；
- 不显示排行榜，避免小组压力和比较。

## 9. 人工确认项

进入 M3 前建议确认：

- 《新生活》前四课授权范围；
- 是否允许产品化改编；
- 是否允许 AI/RAG；
- 是否允许商业化；
- 是否允许再分发；
- 是否允许展示经文译文正文；
- 若展示经文，采用哪个译本；
- 是否使用 YouVersion Platform；
- 神学审核负责人；
- 版权审核负责人；
- 安全审核负责人；
- 首发课程正式名称；
- 每周课次数；
- 每周任务数量；
- 每日任务类型；
- 任务完成是否允许跳过；
- 私人记录是否需要字段级加密；
- 分享给 Leader 是否需要额外提示；
- 30 秒误发撤回是否在 M3 实现，还是 M4/M6 实现。

## 10. 推荐执行提示词

后续可以直接使用以下任务说明开始 M3：

```text
请执行 M3｜课程、任务与私人记录。

开始前：
1. 读取 AGENTS.md、README、docs/M2_GROUPS_INVITATIONS_REPORT.md、docs/product、docs/adr 和 下一步建议2到3.md。
2. 检查当前仓库状态，保留现有修改。
3. 先只读检查当前 Supabase helper、M1/M2 migration、小组权限、认证流程和测试配置。
4. 给出不超过 8 项的实施计划。

实施范围：
- 新增课程、课程版本、模块、课次、内容块、审核记录 migration。
- 新增班次、课次解锁、任务、任务完成记录 migration。
- 新增私人记录 entries 和 content_shares migration。
- 所有新增客户端可访问表开启 RLS。
- 导入《新生活》前四课的产品化 seed 内容，但不导入未经确认的教材全文、经文译文正文、图片、诗歌或外链。
- 每个内容块保留来源定位、版权审核状态、神学审核状态和 AI 准入状态。
- 实现 Leader 为本组创建课程班次和设置开始日期。
- 实现成员查看周课程、课次和任务。
- 实现任务完成、跳过和个人进度。
- 实现私人记录创建、编辑、删除、分享、撤回。
- 默认 private；Leader 不能读取私人正文。
- 补齐 Alice/Bob/Carol/Dave 越权测试。
- 补齐关键 E2E：Leader 创建班次，Bob 完成任务并保存私人记录，Alice 无法读取 Bob 私人正文，Bob 主动分享后 Alice 可读，撤回后 Alice 不可读。

不要：
- 不实现组内讨论、代祷、完整 Leader 面板或 AI。
- 不导入未授权经文译文正文。
- 不把私人记录加入 AI 上下文、RAG、分析或日志。
- 不显示组员排行榜。
- 不把权限只放在前端。

验证：
- npm run lint
- npm run typecheck
- npm run test
- npm run build
- 运行 Alice/Bob/Carol/Dave 越权测试
- 运行课程任务和私人记录 E2E

完成后输出：
- 实现结果
- 变更文件
- 数据库迁移
- 测试结果
- 安全/隐私影响
- 已知限制
- 下一步建议
```

