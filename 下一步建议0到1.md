# 下一步建议：从 M0 到 M1

## 1. 当前状态

项目已经完成 **M0 项目基线**：

- Next.js App Router + TypeScript 严格模式已建立；
- Tailwind 移动端优先 UI 基线已建立；
- lint、typecheck、unit test、build 已通过；
- Playwright E2E 基线已配置并验证通过；
- 产品文档副本和 ADR 已建立；
- 尚未实现认证、小组、数据库、课程、讨论、代祷或 AI。

下一步建议进入 **M1｜认证、同意与边界**。

## 2. M1 总目标

建立用户进入产品前的身份、成人确认、法律同意和平台边界基础。

M1 完成后，用户应能够：

- 使用邮箱注册、登录、退出；
- 确认已满 18 岁；
- 分别同意服务条款、隐私政策和敏感数据处理；
- 创建和维护基础个人资料；
- 从页脚和设置入口访问正式版本化法律/信仰/边界页面；
- 在未完成必要同意前不能继续进入后续小组流程。

## 3. 建议优先级

### 3.1 先做 Supabase 初始化

建议先完成：

- 创建 Supabase 项目或本地 Supabase 配置；
- 添加环境变量；
- 建立 Supabase client/server helper；
- 创建 `supabase/migrations` 下第一批 migration；
- 确认 `service_role` 不进入客户端 bundle；
- 确认所有客户端可访问表默认开启 RLS。

建议新增环境变量：

```env
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```

### 3.2 再做身份与资料表

建议创建：

- `profiles`
- `legal_documents`
- `user_consents`

`profiles` 最小字段：

- `user_id`
- `display_name`
- `avatar_path`
- `timezone`
- `locale`
- `adult_confirmed_at`
- `created_at`

`legal_documents` 最小字段：

- `id`
- `document_type`
- `version`
- `locale`
- `content_hash`
- `published_at`
- `retired_at`

`user_consents` 最小字段：

- `user_id`
- `legal_document_id`
- `consent_type`
- `granted_at`
- `revoked_at`
- `evidence_meta`

### 3.3 再做 RLS 和权限测试

M1 必须验证：

- 未登录用户不能读取 `profiles`；
- 用户只能读取和更新自己的 `profile`；
- 普通客户端不能伪造同意时间；
- 普通客户端不能伪造同意版本；
- 已撤销同意后，后续流程必须重新阻断；
- `service_role` 仅在服务端受控使用。

### 3.4 最后做前端流程

建议页面和流程：

- `/signup`
- `/login`
- `/settings/profile`
- `/settings/privacy`
- `/settings/data`
- 法律/信仰/边界正式页面

注册流程建议顺序：

1. 邮箱注册；
2. 基础资料；
3. 成人确认；
4. 必需法律同意；
5. 敏感数据处理同意；
6. 进入等待邀请或接受邀请流程。

## 4. M1 不建议提前做的内容

M1 阶段仍然不建议做：

- 小组创建和邀请；
- 课程任务；
- 私人灵修笔记；
- 讨论区；
- 代祷；
- Leader 面板；
- AI 问答；
- 数据导出自动化；
- 正式生产部署。

这些功能依赖 M1 的认证、同意和 RLS 基础。

## 5. 验收标准

M1 完成建议至少满足：

- `npm run lint` 通过；
- `npm run typecheck` 通过；
- `npm run test` 通过；
- `npm run build` 通过；
- 注册、登录、退出主流程可用；
- 未完成必需同意时无法继续；
- 同意记录指向具体法律文档版本和内容 hash；
- 营销类同意默认关闭；
- 认证错误不泄露邮箱是否存在；
- RLS 测试覆盖 profile 和 consent 的基本越权场景；
- 页脚可访问正式法律、隐私、信仰和边界页面；
- 日志不记录邮箱、祷告、反思、AI 问答等敏感正文。

## 6. 风险与人工确认项

进入 M1 前建议人工确认：

- 产品正式中文名；
- 产品正式英文名；
- 域名；
- Logo 或最小视觉标识；
- 服务条款正式文本；
- 隐私政策正式文本；
- 敏感数据处理说明；
- 信仰宣言正式文本；
- Leader 盟约正式文本；
- 授权资料覆盖的地区、语言、改编、AI/RAG 和商业化范围；
- 首发圣经译本或 YouVersion 接入策略。

## 7. 推荐执行提示词

后续可以直接使用以下任务说明开始 M1：

```text
请执行 M1｜认证、同意与边界。

开始前：
1. 读取 AGENTS.md、README、docs/M0_PROJECT_BASELINE_REPORT.md、docs/product 和 docs/adr。
2. 检查当前仓库状态，保留现有修改。
3. 先只读检查当前 Next.js 项目结构、环境变量、测试配置和 Supabase 目录。
4. 给出不超过 8 项的实施计划。

实施范围：
- 初始化 Supabase 客户端和服务端 helper。
- 添加 profiles、legal_documents、user_consents migration。
- 为客户端可访问表开启 RLS。
- 实现邮箱注册、登录、退出和基础资料流程。
- 实现成人确认和必需同意流程。
- 将使命、信仰、边界、隐私、条款、社区守则页面替换为正式版本化页面。
- 补齐单元测试、RLS/权限测试和关键 E2E。

不要：
- 不实现小组、课程、讨论、代祷或 AI。
- 不导入课程全文或经文译文。
- 不把 service_role 或 AI key 暴露到客户端。
- 不记录敏感正文到日志或测试快照。

验证：
- npm run lint
- npm run typecheck
- npm run test
- npm run build
- 运行认证和 RLS 越权测试

完成后输出：
- 实现结果
- 变更文件
- 数据库迁移
- 测试结果
- 安全/隐私影响
- 已知限制
- 下一步建议
```

